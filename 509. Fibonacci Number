var fib = function (n) {
    if(n<=1) return n
    let fib = new Array(n+1);
    fib[0] = 1;
    fib[1] = 1;

    for (i = 2; i <= n; i++) {
        fib[i] = fib[i - 1] + fib[i - 2];
    }
    return fib[n-1]

};
