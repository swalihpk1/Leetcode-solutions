var averageValue = function (nums) {
    let count = 0;
    let sum = 0;
    for (let num of nums) {
        if (num % 6 == 0) {
            sum += num
            count++
        }
    }
    if (count === 0) {
        return count;
    }

    return Math.floor(sum / count);
};
