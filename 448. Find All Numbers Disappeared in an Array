var findDisappearedNumbers = function(nums) {
    let arr = new Set(nums);
    let result= [];
    for(let i = 1;i<= nums.length;i++){
    if(!arr.has(i)){
            result.push(i)
        }
    }
    return result;

};
